meta {
  name: POST Health Check (Cron)
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/cron/health-check
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "monitorId": "{{monitorId}}"
  }
}

vars:pre-request {
  baseUrl: http://localhost:3000
}
